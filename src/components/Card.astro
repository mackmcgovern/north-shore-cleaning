---
import { Image } from 'astro:assets'
const { image, name, desc, imgAlt } = Astro.props
---

<div class='flex flex-col'>
  <Image src={image} alt={imgAlt} />
  <p>{name}</p>
  <p>{desc}</p>
</div>
